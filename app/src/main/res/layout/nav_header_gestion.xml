<?xml version="1.0" encoding="utf-8"?>

    <!--- android:background="@drawable/side_nav_bar" -->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="#6D0164"
    android:gravity="bottom"
    android:adjustViewBounds="true"
    android:scaleType="fitXY"
    android:cropToPadding="true"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

<!--
    android:background="@mipmap/img_nav_header"
-->



   <!-- <ImageView
        android:id="@+id/imageViewHeaderNavGestion"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:adjustViewBounds="true"
        android:cropToPadding="true"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:src="@mipmap/beya_logo"
        android:layout_gravity="left"
        android:visibility="gone"/>-->

    <ImageView
       android:id="@+id/imageViewHeaderNavGestion"
       android:layout_width="50dp"
       android:layout_height="50dp"
       android:adjustViewBounds="true"
       android:cropToPadding="true"
       android:src="@drawable/header"
       android:layout_gravity="left"
       android:visibility="visible"/>


    <TextView
        android:id="@+id/NombreUserHeaderNavGestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:text="Android Studio"
        android:textColor="#FFFFFF"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <TextView
        android:id="@+id/EmailHeaderNavGestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:textColor="#FFFFFF"
        android:text="android.studio@android.com" />

</LinearLayout>
